---
title: Making a data subset
---

Once the `request.yaml` has been created by a
[Requester](/docs/design/architecture.qmd#requester)---via the [web
application](/docs/guide/) or the [TUI]()---and accepted by an
[Owner](/docs/design/architecture.qmd#owner), the Owner can use it to create the
requested data package subset. This guide is written for you as the Owner of the
data package.

<!-- TODO: Add links above -->

<!-- TODO: The following should follow nicely from the guides for making the web app and the request. -->

To make a data subset that contains only the resources, columns, and rows
included in the request, we can use Propagate's [`subset`]() command. This
command needs the following input:

<!-- TODO: Add link to subset's ref doc above when made -->

1. The path to the request, a `request.yaml` file. Required; it has no default
   value.
2. The path to the metadata source file (which includes paths to the data).
   Defaults to `datapackage.json` in the current directory.
3. The output directory. Defaults to `subsets/` in the same directory as the
   `request.yaml` file.

<!-- TODO: Add link to build's ref doc above when made -->

All three arguments take both relative and absolute paths.

## Creating a subset with default arguments

Since the only required argument is the request file path, that's all we need to
provide. From the root of your data package, run the `subset` command like so:

```bash
propagate subset path/to/request.yaml
```

With this command, Propagate looks for a `datapackage.json` file in the current
directory and outputs the created subset in a `subsets/` directory in the same
directory as the `request.yaml` file. In the example above, the subset will be
placed in a `path/to/subsets` directory.

Internally, Propagate checks the request against the data package metadata file
before creating the subset. This check can also be performed on its own with
Propagate's [`check`]() command.

<!-- TODO: Add a guide for the `check` command and link to it above. -->

From the request file, Propagate uses the project `name` to create a
`<project-name>_<timestamp>.tar` file with the subset, saved in the `subsets/`
directory. For example, a request with a project name `diet-diabetes` requesting
data from two resources, `participants` and `biomarkers`, produces:

```text
subsets/
└── diet-diabetes_2025-01-01T121000.tar
```

::: content-hidden
Above dir tree built from <https://tree.nathanfriend.com/>.

```text
subsets/
  diet-diabetes_2025-01-01T121000.tar
```
:::

The `.tar` file will contain one Parquet file per requested resource. In this
example, that means:

- `participants.parquet`
- `biomarkers.parquet`

::: callout-note
Each subset output file created by Propagate only contains the columns and rows
requested. For example, if the request contains:

- The `age` and `sex` columns from `participants` and only rows where `sex` is
  `women`, and
- The `hba1c` field from `biomarkers` and only rows with a value above 48.

Then `participants.parquet` will have just the `age` and `sex` columns for those
rows, and `biomarkers.parquet` will have just the `hba1c` column for those rows.

Importantly, if `participants` has a primary key `id` that maps to a foreign key
`participant_id` in `biomarkers`, only rows in `biomarkers` with
`participant_id` values that are also in the filtered `participants` are
included.
:::

::: callout-tip
If you want to see the steps Propagate runs **before** it actually creates the
subset, run the `subset` command with `--show-plan true`. This prints those
steps without executing them.
:::

## Creating a subset with custom arguments

Let's say we want to create a subset using a request file located at
`requests/diet-diabetes/request.yaml` and a metadata source file at
`metadata/datapackage.json`, and we want to place the created subset in a
`diabetes-subsets/` directory. The command for that would be:

```bash
propagate subset requests/diet-diabetes/request.yaml -s metadata/datapackage.json -o diabetes-subsets/
```

This will take the request file in the `requests/diet-diabetes/` directory,
check it against the metadata file in the `metadata/` directory, and create the
subset in the `diabetes-subsets/` directory. If the request's project name is
`diet-diabetes`, the subset directory structure will be:

```text
diabetes-subsets/
└── diet-diabetes_2025-01-01T121000.tar
```

::: content-hidden
Above dir tree built from <https://tree.nathanfriend.com/>.

```text
diabetes-subsets/
  diet-diabetes_2025-01-01T121000.tar
```
:::

As in the previous example, the `.tar` file will contain one Parquet file per
requested resource.

## Next steps

After the subset has been created, it's up to you, the
[Owner](/docs/design/architecture.qmd#owner), to decide how to share it with the
[Requester](/docs/design/architecture.qmd#requester) and any other project
members who need it, following your institution's practices for handling this
kind of data.
